# Tailscale setup (Windows)

1. Install Tailscale on laptop and phone.
2. Join both devices to the same tailnet.
3. Keep laptop online.
4. Optional: expose local API via Tailnet IP `http://100.x.y.z:8787`.

Telegram bot mode does not require inbound ports.
